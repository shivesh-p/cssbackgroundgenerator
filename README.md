# CSSBackgroundGenerator
This is a background generator using the HTML CSS and JS to generate a backgroudn using the linear-gradient property of the CSS and a random button to generate a background by random creation.
